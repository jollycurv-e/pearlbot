🔎 = Feature/functionality missing that was present previously

🆕 = New feature/functionality

🐛 = Working, needs a bug fix

✅ = Complete

❌ = Rejected

⏸️ = On hold

* ❌ ~~offline storage viewing~~ // implemented as Fabric mod (`alt-offline-storage-viewer`); captures player/llama/chest_boat/ender_chest inventories + shulker contents to JSON; HTML viewer with localStorage cache
* ✅ ~~Allow each slot to specify a block type that should be "interacted" with after a successful pearl (intended to "use" a button to dispense a pearl)~~